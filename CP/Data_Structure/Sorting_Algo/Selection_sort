#include<iostream>
using namespace std;

int find_LOC(int arr[], int i, int n)
{
    int MIN = arr[i], LOC = i;
    for(int j = i+1; j<=n; j++)
    {
        if(MIN > arr[j]){
            LOC = j;
            MIN = arr[j];
        }
    }
    return LOC;
}

int main()
{
    int T;
    cin >> T;
    while(T--)
    {
        int n, arr[100], LOC;
        cin >> n;
        for(int i = 1; i<=n; i++)
            cin >> arr[i];
        for(int i = 1; i<=n; i++)
        {
            LOC = find_LOC(arr, i, n);
            swap(arr[i], arr[LOC]);
        }
        for(int i = 1; i<=n; i++)
            cout << arr[i] << " ";
        cout << endl << endl;
    }
    return 0;
}
